<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permiso requerido para recibir comunicación por red local (UDP) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Declarar que es una app de TV -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <!-- No requiere pantalla táctil -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name="mx.utng.bgma.smarthealthmonitor.tv.SmartHealthTVApp"
        android:allowBackup="true"
        android:theme="@style/Theme.SmartHealthTV"
        android:banner="@drawable/banner_tv"
        android:label="SmartHealth TV"
        android:supportsRtl="true">

        <activity
            android:name="mx.utng.bgma.smarthealthmonitor.tv.TVActivity"
            android:exported="true"
            android:label="SmartHealth TV"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Categoría LEANBACK_LAUNCHER: aparece en launcher de Android TV -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Servicio local para recibir mensajes de simulación Wear OS / App móvil en la TV -->
        <service
            android:name="mx.utng.bgma.smarthealthmonitor.tv.WearListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:scheme="wear"
                    android:pathPrefix="/smarthealthmonitor" />
            </intent-filter>
        </service>

    </application>

</manifest>